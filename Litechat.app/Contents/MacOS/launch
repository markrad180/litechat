#!/bin/bash
# Double-click launcher: opens a Terminal window running the repo's start script.
ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
osascript \
	-e 'tell application "Terminal" to activate' \
	-e "tell application \"Terminal\" to do script \"cd '$ROOT' && ./start.sh && exit\""
